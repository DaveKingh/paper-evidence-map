# Method Lens

Use for a requested method, module, mechanism, architecture, or implementation choice.

Reconstruct only the necessary chain:

`input -> operation -> output -> dependency`

Prefer Methods, Algorithm, Equation, and directly relevant diagrams. Inspect ablations or implementation evidence only when they materially affect the requested mechanism explanation. Official code is implementation evidence and must not silently fill details the paper did not report.
